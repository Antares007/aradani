#include "gorgodandgot.h"
#include "oars.h"
#include <stdio.h>
N(ada);
N(os_new);
N(os_queue);

S(and) { A(010) ada(T()); }

SP(ray_not) { C(2); }
SP(ray_and) { A3(god, ο, os_queue) O; }
SP(ray_oor) { C(0); }

NP(seven) { A7(god, ray_not, ray_and, ray_oor, os_new, os_queue, and) O; }
