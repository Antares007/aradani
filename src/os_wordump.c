#include "oars.h"
#include <stdio.h>

NP(os_wordump) {
  long i = 0;
  while (i < α) {
    printf("%016lx ", σ[i].Q);
    if (++i < α) printf("%016lx\n", σ[i].Q);
    else printf("\n");
    i++;
  }
  C(1);
}
