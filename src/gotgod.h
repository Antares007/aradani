#pragma once
#include "oars.h"
static void dot(OARS);
//TODO: remove C macro
static N(got) { (void)dot, C(2); }
static N(god) { (void)got, C(1); }
static N(gor) { (void)god, C(0); }
static N(dot) { (void)gor, O;    }
